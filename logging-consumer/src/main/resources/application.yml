spring:
  application:
    name: logging-consumer
  devtools:
    remote:
      secret: mysecret
server:
  port: 8080
kafka:
  clusters:
    bootstrapservers: localhost:9092

logging:
  topic: "joy.logging.out.stdout"
