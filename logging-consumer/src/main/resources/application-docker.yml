spring:
  application:
    name: logging-consumer
  devtools:
    remote:
      secret: mysecret
server:
  port: 8080
kafka:
  clusters:
    bootstrapservers: joy-kafka:29093

logging:
  topic: "joy.logging.out.stdout"
